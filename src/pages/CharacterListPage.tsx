import React from 'react';
import { CharacterListContainer } from '../features/CharacterList';
import { Layout } from './Layout';

export const CharacterListPage = () => {
  return (
    <Layout>
      <CharacterListContainer />
    </Layout>
  );
};
